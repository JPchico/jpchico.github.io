import { BoldGivenStringPipe } from './bold-given-string.pipe';

describe('BoldGivenStringPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldGivenStringPipe();
    expect(pipe).toBeTruthy();
  });
});
